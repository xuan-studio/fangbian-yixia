# 参与贡献

感谢你帮助维护一张更可信的城市厕所地图。

## 开始之前

1. 阅读 `README.md`、`AGENTS.md` 和 `AI-DEPLOY.md`。
2. 从 `main` 创建功能分支。
3. 安装依赖并确认基线测试通过：

```bash
npm ci
npm run lint
npm test
```

## 数据贡献规则

- 新记录必须说明公开来源或现场验证方式。
- 没有证据的字段保持 `null`，不要推测楼层、设施或开放状态。
- Mock 数据必须明确标注，不得混入正式评分或可信度。
- 不提交原始 Cookie、账号信息、精确个人轨迹或个体健康记录。
- OpenStreetMap 数据更新必须保留 ODbL 与贡献者署名。
- 第三方平台内容只提交必要的结构化事实、短摘录和来源链接，不复制整篇帖子。

## Pull Request

PR 请写清：改了什么、为什么改、数据来源、用户影响和验证结果。界面修改应同时检查 390px 手机宽度与桌面视口。

提交前再次运行：

```bash
npm run lint
npm test
```
